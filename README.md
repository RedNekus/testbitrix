# 📊 Bitrix24 Companies Fetcher

Инструмент для выгрузки компаний из Bitrix24 через REST API с возможностью поиска, фильтрации и экспорта в Excel.

![Interface Preview](https://via.placeholder.com/900x400/f5f5f5/333333?text=Bitrix24+Companies+Fetcher+Interface)

## ✨ Возможности

- **Выгрузка до 10 000 компаний** из Bitrix24
- **Постраничный прогресс** загрузки (с индикацией %)
- **Поиск по названию компании** в реальном времени
- **Экспорт в Excel** без серверной обработки
- **Кэширование на 10 минут** — данные сохраняются при обновлении страницы
- **Полная локализация ошибок** Bitrix24 с инструкциями
- **Поддержка всех локальных версий** Bitrix24 (`.by`, `.kz`, `.ua`, `.com.tr` и др.)
- **Адаптивный интерфейс** для мобильных устройств

## 🛠️ Требования

- **PHP 7.4+** с включённым расширением `cURL`
- **Composer** для установки зависимостей
- **Веб-сервер** (Apache/Nginx или встроенный PHP-сервер)
- **Браузер** с поддержкой `localStorage` и современного JavaScript

## 🚀 Быстрый запуск

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/your-username/bitrix24-companies-fetcher.git
cd bitrix24-companies-fetcher
```

### 2. Установите зависимости
```bash
composer install
```

### 3. Запустите встроенный сервер PHP
```bash
php -S localhost:8000
```

### 4. Откройте в браузере
```
http://localhost:8000/index.php
```

## 🔧 Как получить URL вебхука Bitrix24

1. Зайдите в ваш портал Bitrix24
2. Перейдите в: **Настройки → Интеграции → REST API**
3. Нажмите **«Создать новый вебхук»**
4. Укажите название (например, *«Выгрузка компаний»*)
5. Выберите права:
   - **CRM → Компании → Просмотр**
6. Скопируйте готовый URL вида:  
   `https://ваш-портал.bitrix24.by/rest/123/токен/crm.company.list.json`

⚠️ **Важно**: URL должен содержать `/crm.company.list.json` в пути!

## 📂 Структура проекта

```
/bitrix24-companies-fetcher/
├── composer.json          # Зависимости PHP
├── index.php              # Точка входа (Twig)
├── api.php                # Прокси для Bitrix24 API
├── public/                # Публичные ресурсы
│   ├── css/
│   │   └── styles.css     # Стили с CSS Variables
│   └── js/
│       └── app.js         # Vue.js логика
└── templates/
    └── main.twig          # Шаблон интерфейса
```

## ⚙️ Особенности реализации

### Кэширование
- Данные сохраняются в `localStorage` на **10 минут**
- Кэш привязан к конкретному URL вебхука
- При обновлении страницы данные восстанавливаются автоматически
- Кнопка **«Очистить кэш»** сбрасывает всё состояние

### Обработка ошибок
Система распознаёт и локализует ошибки Bitrix24:
- `insufficient_scope` → «У вебхука нет прав на просмотр компаний...»
- `invalid_token` → «Токен вебхука недействителен...»
- HTTP 5xx → «Сервер Bitrix24 временно недоступен...»

### Безопасность
- Валидация URL перед отправкой
- HTTPS-требование для всех подключений
- Ограничение частоты запросов (задержка 50 мс)
- Таймаут запросов 50 секунд

## 🔄 Обновление стилей

Все стили используют **CSS Custom Properties**. Для изменения темы отредактируйте файл:

```
public/css/styles.css
```

Основные переменные в блоке `:root`:
```css
:root {
  --primary: #007bff;        /* Основной цвет */
  --error: #dc3545;          /* Цвет ошибок */
  --bg-page: #f5f5f5;        /* Фон страницы */
  /* ... */
}
```

## 💻 Совместимость

| Браузер | Поддержка |
|---------|-----------|
| Chrome | ✅ 50+ |
| Firefox | ✅ 48+ |
| Safari | ✅ 10+ |
| Edge | ✅ 79+ |

## 📝 Лицензия

Этот проект распространяется под лицензией MIT — см. файл [LICENSE](LICENSE).

---

## 🆘 Возникли проблемы?

1. **Проверьте URL вебхука** — он должен содержать `/crm.company.list.json`
2. **Убедитесь в правах** — у токена должны быть права на просмотр компаний
3. **Откройте консоль браузера** — там могут быть ошибки CORS или синтаксиса
4. **Проверьте PHP-ошибки** — включите `display_errors` в `php.ini` при разработке

> 💡 **Совет**: для тестирования используйте **инкогнито-режим**, чтобы исключить конфликты с кэшем.

---

## 👥 Автор

Разработан как тестовое задание  
**Автор**: [RedNekus](https://github.com/RedNekus) 